import 'package:flutter/material.dart';

// Definindo variáveis de cores
const Color primaryColor = Color(0xFFFD6E4E);
const Color backgroundColor = Color(0xFF393D46);
const Color colorInput = Color(0xFF202328);
const Color textColor = Color(0xFFF0F0F0);
